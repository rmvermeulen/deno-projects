{
  "name": "my-app",
  "description": "A simple deno API",
  "version": "1.0.0",
  "main": "main.ts",
  "author": "Rasmus Vermeulen <vermeulen.rasmus@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "deno run --config=tsconfig.json --unstable --importmap=importmap.json --allow-net src/main.ts",
    "test": "deno test --allow-net --config=tsconfig.json --unstable --importmap=importmap.json tests/*.test.ts"
  }
}